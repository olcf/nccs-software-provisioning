    argument_specs:
      main:
        description:
          - Installs the AOCC compiler and optionally generates a module file.
        options:
          NSP_AOCC_version:
            type: str
            required: true
            description: The version to install.
