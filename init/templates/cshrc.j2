#!/usr/bin/env csh
{{ NSP_template_header | comment(beginning='##', end='##', decoration='#', prefix_count=0, postfix_count=0) }}

########################################
# NSP Helpers
########################################

alias nsp_initialize_lmod 'if ( -f {{ [NSP_install_root, "lmod/etc/cshrc"] | path_join }} ) source {{ [NSP_install_root, "lmod/etc/cshrc"] | path_join }}'

########################################
# User Content
########################################

{{ lookup('template', [playbook_dir, 'init/cshrc.j2'] | path_join, errors='ignore') | default('') }}
