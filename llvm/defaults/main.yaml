# NSP_LLVM_version:

NSP_LLVM_targets:
  - X86

NSP_LLVM_projects:
  - clang
  - lld
  - compiler-rt

NSP_LLVM_runtimes:
  - libcxx
  - libcxxabi
  - libunwind
  - openmp

NSP_LLVM_create_module: true

NSP_LLVM_clear_source: true
